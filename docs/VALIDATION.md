# 验证记录

日期：2026-09-12；版本：1.0.0。

## 自动检查

`npm run check` 通过，10 项 Node.js 测试：

- 转换保留原对象与额外 UI 字段。
- 重复转换不累积补丁。
- 已删除的 Agent 强制隐藏选项从旧配置传入也不生效。
- 关闭布局修复后保留原几何，不处理非目标选择器。
- 仅在主题明确声明悬浮输入栏时加入定位补丁。
- 拒绝角色卡与不合法 CSS 字段，接受纯颜色主题。
- 风险分析重复调用保持稳定。
- 宿主主题读取隔离对象并排除无关设置。
- 保存核实比对主题名称与 CSS。
- 未连接、HTTP 失败与不支持的返回结构给出错误。

esbuild 成功输出自包含 JS 与可导入脚本 JSON；运行时不依赖 node_modules、CDN 或预览模拟宿主。

## 浏览器人工验证（模拟宿主）

- 读取四款示例美化，选中另一款时原生主题下拉框仍保留原值。
- 点击“生成并应用”后创建独立 TT 适配副本，显示副本保存核实成功。
- 非法 JSON 主题结构显示读取失败并禁用操作；随后上传有效主题可恢复。
- 点击“仅下载适配版”产生下载动作及生成反馈，无脚本错误；本次内嵌浏览器未提供下载完成事件，因此未据此确认系统下载目录落盘。
- 检查 320px、390px 手机与 1280px 桌面排版；内容区域没有横向溢出。
- 方向键切换美化来源标签正常，诊断导出操作产生成功反馈且没有运行错误。
- 关闭与重新打开正常，保留本次选择。
- 桌面/手机截图保存于本目录，均为明确标注的演示数据。
- 扩展打包结构已检查：根目录存在 `manifest.json`、`index.js`、`style.css`，manifest 指向可加载入口。

## 真实酒馆回归（待维护者执行）

本环境没有连接实际酒馆，以下尚未验证，不能视为已经通过：

- 酒馆助手全局脚本导入、旧版替换与启停清理。
- ST / TT 真实已导入美化列表读取与 CSRF 处理。
- 原生文件导入、`@import` 确认/取消、保存失败与断网恢复。
- 刷新酒馆和切换聊天后，TT 的全局/角色/聊天主题绑定行为。
- iOS/Android 的软键盘、刘海安全区、横竖屏、悬浮输入栏触屏展开。
- 各移动 WebView 的下载保存与系统分享面板。

发现问题时优先提交最小主题 CSS 与经检查的布局诊断，不需要提交聊天内容或完整酒馆设置。
