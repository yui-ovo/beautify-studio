{
  "name": "beautify-studio",
  "version": "1.0.5",
  "private": true,
  "type": "module",
  "description": "美化工作室 · SillyTavern / TauriTavern UI 美化适配工具",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
