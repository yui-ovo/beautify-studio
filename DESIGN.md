# 美化工作室 · 设计规范

## 方向

银灰、黑白、半透明卡片构成的小型个人工作台。参考用户提供的 INS 拼贴排版：窄黑条、四角星、层叠的小卡片、精细英文标记。主要操作和说明使用中文。

## 视觉变量

| 用途 | 值 |
| --- | --- |
| 主文字 | `#272829` |
| 次文字 | `#737577` |
| 面板 | `#fafafa` |
| 边线 | `#dedfe0` |
| 主按钮 | `#292b2c` |
| 卡片圆角 | 20px |
| 胶囊 | 24–30px |
| 内容间距 | 12 / 18 / 24 / 32px |

中文使用系统无衬线字体；英文装饰使用 Georgia 和等宽字体。无外部字体请求。细小英文仅用于装饰，操作控件依靠中文名称。实际阅读内容至少 12px，标题 14–27px；装饰索引 8–10px。

## 布局

- 桌面：左侧选择美化，右侧选项与检查，底部操作。
- 700px 以下：单列；面板内部滚动，尊重手机安全区。
- 美化卡只呈现名称和安全的颜色示意；不执行主题 CSS、不伪造实际截图。
- 主操作只有“生成并应用”，下载和诊断为次操作。
- 黑白层次为主；状态色只用于错误、成功和兼容提示。

## 交互

采用原生 button/input，支持键盘焦点、切换标签、Esc 关闭、焦点循环与恢复。状态以 `aria-live` 播报。按钮禁用必须反映实际可用性。动画仅使用轻微过渡，尊重 `prefers-reduced-motion`。

## 维护

样式只改 `src/ui/studio.css`，结构改 `src/ui/markup.js`。不要直接编辑 dist。继续遵循本规范，避免回退到紫色渐变、密集长说明、暗色表单堆叠。
